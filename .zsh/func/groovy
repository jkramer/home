# vim:ft=zsh

if [ -d "$HOME/bloat/groovy" ]; then
	export GROOVY_HOME="$HOME/bloat/groovy"
	
	groovy_jar_list=($GROOVY_HOME/lib/*)

	export CLASSPATH="$CLASSPATH:${(j|:|)groovy_jar_list}:."
	export PATH="$PATH:$GROOVY_HOME/bin"
fi
